require "spec_helper"

RSpec.describe Cap::Ssh::Cmd do
  it "has a version number" do
    expect(Cap::Ssh::Cmd::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
